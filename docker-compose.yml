services:
  ui:
    build: ./ui/
    ports:
      - "9000:9000"
    volumes:
      - ./ui/scheduler_ui:/app
      - /app/node_modules

  api:
    build: ./api/
    ports:
      - "8000:8000"
    volumes:
      - ./api/:/app
