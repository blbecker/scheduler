meta {
  name: postSkill
  type: http
  seq: 2
}

post {
  url: http://localhost:8000/v1/skills
  body: json
  auth: inherit
}

body:json {
  {
    "id": "{{$randomUUID}}",
    "name": "{{$randomBsAdjective}} {{$randomBsNoun}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
